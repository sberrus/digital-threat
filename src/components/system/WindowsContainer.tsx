import { Container } from "react-bootstrap";

const WindowsContainer = () => {
	return (
		<Container fluid className="p-0" style={{ height: "93%" }}>
			WindowsContainer
		</Container>
	);
};

export default WindowsContainer;
